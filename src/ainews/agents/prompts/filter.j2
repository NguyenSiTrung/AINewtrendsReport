You are a relevance scorer for an AI news intelligence pipeline.

Score the following article for relevance to the given topics.

## Article
- **Title:** {{ title }}
- **Content:**
{{ content }}

## Target Topics
{% for topic in topics %}
- {{ topic }}
{% endfor %}

## Instructions
1. Score the article's relevance on a scale of 0.0 to 1.0.
2. Consider: topic alignment, recency signals, technical depth, and actionability.
3. Articles about AI infrastructure, models, policy, or industry moves are high-value.
4. Generic tech news with only passing AI mentions should score low.

## Output Format
Respond with ONLY a JSON object:
```json
{"score": 0.85, "keep": true, "reason": "Directly discusses new LLM architecture"}
```

Set `keep` to `true` if score >= 0.5, `false` otherwise.
